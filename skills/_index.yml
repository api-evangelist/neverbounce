generated: '2026-08-13'
method: generated
source: >-
  openapi/neverbounce-*-openapi.yml (harvested from the definition NeverBounce publishes
  behind its interactive API reference) plus the NeverBounce developer reference
provider: NeverBounce
api: NeverBounce API v4
searched_for_provider_published_skills:
  checked:
  - https://developers.neverbounce.com/llms.txt
  - https://github.com/NeverBounce
  - https://developers.neverbounce.com/AGENTS.md
  result: none
  note: >-
    NeverBounce publishes an llms.txt (saved verbatim to llms/neverbounce-llms.txt) but no
    AGENTS.md and no packaged agent skills. The skills below are generated by API
    Evangelist and grounded entirely in real operationIds from the provider's own OpenAPI
    definition plus its published reference pages.
skills:
- name: neverbounce-verify-single-email
  file: neverbounce-verify-single-email.md
  flow: Real-time verification of one address at the point of capture.
  operations: [single-check, account-info]
- name: neverbounce-verify-a-list
  file: neverbounce-verify-a-list.md
  flow: >-
    Full bulk lifecycle — create, parse, sample, start, wait on callbacks or polling,
    page or download results, delete.
  operations: [jobs-create, jobs-parse, jobs-start, jobs-status, jobs-results, jobs-download, jobs-search, jobs-delete, account-info]
- name: neverbounce-check-credits-and-usage
  file: neverbounce-check-credits-and-usage.md
  flow: Pre-flight the balance and the published concurrency/volume ceilings before spending credits.
  operations: [account-info, jobs-search, single-check]
cross_cutting_rules_every_skill_repeats:
- >-
  Errors arrive as HTTP 200 with a non-`success` `status` field. Never branch on the HTTP
  status alone.
- >-
  No idempotency key exists and /single/check bills per call including duplicates — a
  retry is a repeat charge.
- >-
  Single verification is for interactive events only; iterating a list through it risks
  account lockout.
- >-
  The standard API is not callable from a browser (no CORS); the JavaScript widget is the
  client-side surface.
